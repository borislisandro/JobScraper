import { invoke } from "@tauri-apps/api/core";
export type Source={id:string;name:string;baseUrl:string;adapterId:string;adapterVersion:string;enabled:boolean;kind:string;disabledReason?:string;robotsOverride:boolean;lastSuccessAt?:string};
export type Job={id:string;sourceId:string;title:string;company:string;location?:string;workMode?:string;canonicalUrl?:string;applyUrl?:string;descriptionText:string;postedAt?:string;seniority?:string;availability:string;score?:number;eligible?:boolean;reasons?:string};
export type Persona={id:string;name:string;targetTitlesJson:string;includeKeywordsJson:string;includeKeywordMode:"any"|"all";excludeKeywordsJson:string;threshold:number;unknownPolicy:string}; export type Application={id:string;jobId:string;personaId?:string;currentStage:string;appliedAt?:string;title?:string;company?:string}; export type RestoreStatus={applied:boolean;restart_required:boolean;message:string};export type RestoreStage={restart_required:boolean;staging_path:string;message:string};export type WorkerEvent={event:string;runId:string;payload:Record<string,unknown>};
export type Interview={id:string;applicationId:string;stage:string;scheduledAt:string;notes?:string};
export const scrapeAll=(runId:string)=>invoke<{runId:string;completedSources:number;failedSources:number;cancelledSources:number}>("scrape_all",{runId});
export const cancelScrapeAll=(runId:string)=>invoke<boolean>("cancel_scrape_all",{runId});
export const cancelScrape=(runId:string)=>invoke<boolean>("cancel_scrape",{runId});
export const api={listSources:()=>invoke<Source[]>("list_sources"),saveSource:(input:unknown)=>invoke<Source>("save_source",{input}),deleteSource:(sourceId:string)=>invoke("delete_source",{sourceId,purge:false}),probe:(source:Source)=>invoke<WorkerEvent[]>("probe_source",{source}),adapterManifests:()=>invoke("adapter_manifests"),scrapeAll:()=>invoke<{runId:string;completedSources:number;failedSources:number}>("scrape_all"),jobs:(personaId?:string)=>invoke<Job[]>("list_jobs",{personaId}),search:(query:string,personaId?:string)=>invoke<Job[]>("search_jobs",{query,personaId}),personas:()=>invoke<Persona[]>("list_personas"),savePersona:(input:unknown)=>invoke<Persona>("save_persona",{input}),rescorePersona:(personaId:string)=>invoke("rescore_persona",{personaId}),rescoreStale:(personaId:string,runId:string)=>invoke<{runId:string;completed:number;total:number;cancelled:boolean}>("rescore_stale_matches",{personaId,runId}),cancelRescore:(runId:string)=>invoke("cancel_rescore",{runId}),rescoreMatch:(jobId:string,personaId:string)=>invoke("rescore_match",{jobId,personaId}),matchExplanation:(jobId:string,personaId:string)=>invoke<Record<string,unknown>>("get_match_explanation",{jobId,personaId}),review:(personaId:string)=>invoke<Job[]>("list_review_queue",{personaId}),decide:(jobId:string,personaId:string,status:string,reason?:string)=>invoke("set_review_decision",{jobId,personaId,status,reason}),apps:()=>invoke<Application[]>("list_applications"),createApp:(jobId:string,personaId?:string)=>invoke<Application>("create_application",{jobId,personaId}),stage:(applicationId:string,stage:string)=>invoke<Application>("transition_application",{input:{applicationId,stage}}),openApply:(applicationId:string)=>invoke("open_apply",{applicationId}),pendingApply:()=>invoke<{attemptId:string;applicationId:string;jobId:string;openedAt:string;originalStage:string}|undefined>("pending_apply_confirmation"),applyDecision:(applicationId:string,decision:"yes"|"no"|"not_yet")=>invoke("record_apply_decision",{applicationId,decision}),interviews:(applicationId:string)=>invoke<Interview[]>("list_interviews",{applicationId}),saveInterview:(input:unknown)=>invoke<Interview>("save_interview",{input}),deleteInterview:(interviewId:string)=>invoke("delete_interview",{interviewId}),ghostThreshold:(applicationId:string,days:number)=>invoke("update_ghost_threshold",{applicationId,days}),reconcileReminders:()=>invoke("reconcile_reminders"),resumeScrape:(runId:string)=>invoke("resume_scrape",{runId}),analytics:()=>invoke<{total:number;byStage:{name:string;count:number}[];appliedToResponseHours?:number;responseSamples:number}>("analytics"),diagnostics:()=>invoke<{dbPath:string;schemaVersion:number;sourceCount:number;jobCount:number;startupNetwork:boolean;sidecarActive:boolean}>("diagnostics"),backup:()=>invoke<string>("create_backup_archive"),validateRestore:(archiveBase64:string)=>invoke("validate_restore_archive",{archiveBase64}),stageRestore:(archiveBase64:string)=>invoke<RestoreStage>("stage_restore_archive",{archiveBase64}),restoreStatus:()=>invoke<RestoreStatus|undefined>("restore_status"),export:(kind:string)=>invoke<string>("export_csv",{kind}),test:(source:Source)=>invoke<WorkerEvent[]>("test_source",{source}),scrape:(source:Source)=>invoke<WorkerEvent[]>("scrape_source",{source})};
